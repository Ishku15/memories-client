import { makeStyles } from "@material-ui/styles";

export default makeStyles(() => ({
    appBar: {
        margin: '30px 0',
        display: 'flex',
        flexDirection: 'row',
        justifyContent: 'center',
        alignItems: 'center',
      },
      heading: {
        color: 'black',
        fontFamily: 'Nunito'
      },
      image: {
        marginLeft: '15px',
      },
}))